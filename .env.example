DATABASE_URL=postgresql://carvalhaesiluminacao:senha@db:5432/carvalhaesiluminacao
DB_USER=carvalhaesiluminacao
DB_PASS=MUDAR_ISTO
SECRET_KEY=GERAR_CHAVE_ALEATORIA
WEBHOOK_SECRET=WEBHOOK_SECRET_DO_GITHUB
ADMIN_USER=admin
ADMIN_PASS=mudar123
CLOUDFLARE_TUNNEL_TOKEN=TOKEN_DO_NOVO_TUNNEL

# ── Orçamentos internos (migrado de com.automacaobbc.ia) ──
# Ollama (GPU do host) — usado só no fallback de visão p/ PDFs escaneados / imagens
# Ollama roda no PC do Bruno (GPU), NAO no servidor: usar o IP da LAN desse PC.
OLLAMA_BASE=http://192.168.1.251:11434
OLLAMA_URL=http://192.168.1.251:11434/api/generate
OLLAMA_MODEL=llama3.1:8b
# Gerador de Orçamentos — modo Link (Google Sheets). OAuth do projeto GCP do ia.
# Adicionar este redirect_uri ao OAuth client no Google Cloud Console.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://carvalhaesiluminacao.automacaobbc.com/api/auth/google/callback
